<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 1. OSM Droid Map View (Full Screen Background) -->
    <org.osmdroid.views.MapView
        android:id="@+id/map_view_osm"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#A0B0C0"
        app:layout_constraintTop_toBottomOf="@id/main_header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 2. Header Bar (Dark Blue) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main_header"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#0A1C3A"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Menu/Profile Button -->
        <ImageView
            android:id="@+id/btn_menu"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="Menu"
            android:src="@drawable/ic_person_black_24dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:tint="@android:color/white" />

        <!-- App Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="RIDEO"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- More Options Button -->
        <ImageView
            android:id="@+id/btn_options"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="Options"
            android:src="@drawable/ic_more_vert_white_24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:tint="@android:color/white" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Overlay Text: Available Carts -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Available Carts: 3"
        android:textColor="@android:color/black"
        android:textSize="14sp"
        android:background="@drawable/rounded_input_field_white"
        android:padding="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toTopOf="@+id/request_card"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 3. Ride Request/Status Bottom Card (Container for both states) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/request_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="20dp"
        app:cardElevation="16dp"
        android:layout_marginStart="0dp"
        android:layout_marginEnd="0dp"
        app:cardBackgroundColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- A. RIDE REQUEST VIEW (Default View) -->
            <LinearLayout
                android:id="@+id/ride_request_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingTop="24dp"
                android:paddingBottom="32dp"
                android:visibility="visible"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- PICKUP LOCATION LABEL -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="PICKUP LOCATION"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:layout_marginBottom="4dp"/>

                <!-- Dropdown for Pickup Location -->
                <androidx.appcompat.widget.AppCompatSpinner
                    android:id="@+id/input_pickup_location"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:spinnerMode="dropdown"
                    android:background="@drawable/rounded_input_field_white"
                    android:paddingStart="16dp"
                    android:paddingEnd="16dp"
                    android:layout_marginBottom="16dp" />

                <!-- DROP-OFF LOCATION LABEL -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="DROP-OFF LOCATION"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:layout_marginBottom="4dp"/>

                <!-- Dropdown for Drop-off Location -->
                <androidx.appcompat.widget.AppCompatSpinner
                    android:id="@+id/input_dropoff_location"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:spinnerMode="dropdown"
                    android:background="@drawable/rounded_input_field_white"
                    android:paddingStart="16dp"
                    android:paddingEnd="16dp"
                    android:layout_marginBottom="24dp" />

                <!-- Request Button -->
                <Button
                    android:id="@+id/btn_request_ride"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:text="REQUEST CART NOW"
                    android:backgroundTint="#0A1C3A"
                    android:textColor="@android:color/white"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    app:cornerRadius="12dp"/>
            </LinearLayout>

            <!-- B. RIDE STATUS VIEW (Initially Hidden) -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/ride_status_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="24dp"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- Main Status Text -->
                <TextView
                    android:id="@+id/status_main_text"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:text="CART MATCHED - ARRIVING SOON!"
                    android:textColor="@android:color/black"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:layout_marginEnd="16dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toStartOf="@id/status_icon_end"
                    android:layout_marginBottom="12dp"/>

                <!-- From/To Details -->
                <LinearLayout
                    android:id="@+id/status_details"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:layout_marginTop="8dp"
                    app:layout_constraintTop_toBottomOf="@id/status_main_text"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toStartOf="@id/status_icon_end">

                    <TextView
                        android:id="@+id/status_from"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="From: Library East Wing"
                        android:textColor="#757575"
                        android:textSize="16sp"/>

                    <TextView
                        android:id="@+id/status_to"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="To: Science Building"
                        android:textColor="#757575"
                        android:textSize="16sp"/>

                    <!-- NEW: Status Message -->
                    <TextView
                        android:id="@+id/status_message"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Waiting for driver..."
                        android:textColor="#0A1C3A"
                        android:textSize="16sp"
                        android:layout_marginTop="8dp"
                        android:textStyle="italic"/>
                </LinearLayout>


                <!-- Icon at the end (briefcase/pin) -->
                <ImageView
                    android:id="@+id/status_icon_end"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_pin_drop_blue_24dp"
                    android:contentDescription="Drop Off Location Pin"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="@id/status_main_text"
                    app:layout_constraintBottom_toBottomOf="@id/status_details"
                    app:tint="#0A1C3A"/>


            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>
